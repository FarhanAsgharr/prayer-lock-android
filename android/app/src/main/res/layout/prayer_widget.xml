<?xml version="1.0" encoding="utf-8"?>
<!--
  Deliberately plain. A home-screen widget is glanced at, not read: the prayer
  name and its time are the only things that need to survive a half-second
  look, so they get the size and the contrast, and everything else is
  secondary.

  Colours come from @android:color/system_* so the widget matches the user's
  wallpaper theme on Android 12+, and fall back to the app's own on older
  releases via values/colors.xml.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:padding="14dp"
    android:background="@drawable/widget_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/widget_prayer"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="17sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/widget_text_primary"
            android:text="@string/widget_placeholder_prayer" />

        <TextView
            android:id="@+id/widget_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:textSize="17sp"
            android:textColor="@color/widget_text_primary"
            android:text="@string/widget_placeholder_time" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_detail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:textSize="13sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="@color/widget_text_secondary"
        android:text="@string/widget_placeholder_detail" />
</LinearLayout>
