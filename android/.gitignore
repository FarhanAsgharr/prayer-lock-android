# Never commit signing secrets. Also covered by the repo-root .gitignore; this
# is a second guard so a partial clone or a tool that ignores the root file
# still cannot stage the keystore or its passwords.
key.properties
*.jks
*.keystore
*.p12

# Gradle build output.
.gradle/
build/
/captures/
